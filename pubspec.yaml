name: marshalling
version: 0.1.4
author: Andrew Mezoni <andrew.mezoni@gmail.com>
description: The marshalling library allows to marshal and unmarshal (also serialize/deserialize) an objects (e.g. into json compatible types)
homepage: https://github.com/mezoni/marshalling
environment:
  sdk: '>=2.3.0 <3.0.0'
dependencies:
  path: ^1.6.2
  yaml: ^2.1.15
dev_dependencies:
  pedantic: ^1.7.0
  test: ^1.6.3